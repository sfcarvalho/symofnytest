{% block body %}

    <div class="page-header">
        <h2>Relatórios </h2>
    </div>

    <small>Por favor, preencha corretamente todos os campos solicitados.</small>
    <div class="form_error"></div>
    <form method="POST" class="reportForm" action="{{path('reports_results')}}">
        <div id="form_body">
            {% include 'ChamadosBundle:Chamados:reportsForm.html.twig' with {'form': form} %}
        </div>
        <button type="submit" class="btn btn-primary buscarChamados">Buscar</button>
        <button type="button" class="btn btn-primary hide novaBusca">Nova Busca</button>
        {{form_rest(form)}}
    </form>
    </div>
{% endblock %}